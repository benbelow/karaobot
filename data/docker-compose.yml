version: '3.1'

services:
  postgres-local-db:
    image: postgres:13.7
    restart: always
    ports:
      - '5438:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: karaobot
